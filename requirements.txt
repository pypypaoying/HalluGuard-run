# Minimal runtime for the HalluGuard run package.
# For CUDA servers, install the appropriate torch wheel first if needed:
# https://pytorch.org/get-started/locally/
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
PyYAML>=6.0
tqdm>=4.66
torch>=2.1
